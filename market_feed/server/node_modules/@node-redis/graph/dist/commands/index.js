"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pushQueryArguments = void 0;
const CONFIG_GET = require("@node-redis/graph/dist/commands/CONFIG_GET");
const CONFIG_SET = require("@node-redis/graph/dist/commands/CONFIG_SET");
;
const DELETE = require("@node-redis/graph/dist/commands/DELETE");
const EXPLAIN = require("@node-redis/graph/dist/commands/EXPLAIN");
const LIST = require("@node-redis/graph/dist/commands/LIST");
const PROFILE = require("@node-redis/graph/dist/commands/PROFILE");
const QUERY_RO = require("@node-redis/graph/dist/commands/QUERY_RO");
const QUERY = require("@node-redis/graph/dist/commands/QUERY");
const SLOWLOG = require("@node-redis/graph/dist/commands/SLOWLOG");
exports.default = {
    CONFIG_GET,
    configGet: CONFIG_GET,
    CONFIG_SET,
    configSet: CONFIG_SET,
    DELETE,
    delete: DELETE,
    EXPLAIN,
    explain: EXPLAIN,
    LIST,
    list: LIST,
    PROFILE,
    profile: PROFILE,
    QUERY_RO,
    queryRo: QUERY_RO,
    QUERY,
    query: QUERY,
    SLOWLOG,
    slowLog: SLOWLOG
};
function pushQueryArguments(args, graph, query, timeout) {
    args.push(graph, query);
    if (timeout !== undefined) {
        args.push(timeout.toString());
    }
    return args;
}
exports.pushQueryArguments = pushQueryArguments;
